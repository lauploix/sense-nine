package com.mechanitis.demo.sense.twitter;

public class UserService {
    /**
     * Calculates the number of items a user can access,
     * including a bonus item.
     */
    public int getAccessibleItemCount(int baseItems) {
        return baseItems;
    }
}